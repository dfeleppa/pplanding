# Public env vars wired into the build at runtime via next/script + metadata.
# Copy this file to `.env.local` and fill in the real values to enable.

# Google Analytics 4 measurement ID, e.g. G-XXXXXXXXXX.
# When unset, the GA4 <Script> tags are not rendered.
NEXT_PUBLIC_GA_ID=

# Google Search Console HTML-tag verification token (just the value, not the
# full <meta> markup). When unset, no verification meta tag is rendered.
NEXT_PUBLIC_GSC_VERIFICATION=

# Optional training giveaway overrides. The approved defaults live centrally
# in src/lib/giveaway-config.ts. Use ISO-8601 timestamps with an explicit
# offset, e.g. 2026-09-01T10:00:00-04:00.
GIVEAWAY_START_AT=
GIVEAWAY_END_AT=
GIVEAWAY_ELIGIBLE_AREA=
GIVEAWAY_WINNER_RESPONSE_DEADLINE=
GIVEAWAY_PRIZE_EXPIRATION=
GIVEAWAY_TRANSFERABILITY=
GIVEAWAY_RULES_VERSION=
